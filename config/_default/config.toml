# set `baseurl` to your root domain
# if you set it to "/" share icons won't work properly on production
baseurl = "https://philippe-assouline.com/"  # Include trailing slash
# canonifyurls = true
paginate = 10
theme = "hugo-clarity"
disqusShortname = ""

DefaultContentLanguage = "en"

[taxonomies]
#category = "categories"
tag = "tags"
#series = "series"

[outputs]
home = ["HTML", "RSS", "JSON"]
